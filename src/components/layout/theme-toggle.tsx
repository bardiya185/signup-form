'use client'; import { Moon,Sun } from 'lucide-react'; import { useEffect,useState } from 'react'; export function ThemeToggle(){const [dark,setDark]=useState(false);useEffect(()=>setDark(document.documentElement.classList.contains('dark')),[]);const toggle=()=>{document.documentElement.classList.toggle('dark');setDark(!dark)};return <button onClick={toggle} className="grid h-10 w-10 place-items-center rounded-xl hover:bg-zinc-100 dark:hover:bg-zinc-800" aria-label="تغییر پوسته">{dark?<Sun size={19}/>:<Moon size={19}/>}</button>}

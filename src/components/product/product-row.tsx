import { Product } from '@/types'; import { ProductCard } from './product-card'; export function ProductRow({title,products,id}:{title:string;products:Product[];id?:string}){return <section id={id} className="container-page mt-10"><div className="mb-5 flex items-center justify-between"><h2 className="section-title">{title}</h2><button className="text-sm font-bold text-brand">مشاهده همه</button></div><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-5 xl:grid-cols-6">{products.map(p=><ProductCard product={p} key={p.id}/>)}</div></section>}
